let obj = {}; //forma de se inicializar um objeto

obj.name = "Julia"; //criação de uma chave com o valor chamado Julia

obj.age = 20;

Object.values(obj); //usado para saber quais valores estão no objeto

Object.keys(obj); //mostra as chaves desse objeto

let person = {
    name: "Julia",
    age: 20,
    adress: "Rua dois"
} //declaração de um objeto com os dados
